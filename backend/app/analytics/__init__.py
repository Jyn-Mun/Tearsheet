"""Deterministic analytics layer — pure Python (numpy), no paid APIs, no LLM.

Everything here is reproducible: each output carries its formula, inputs, and (where relevant)
sample size. The optional LLM layer sits ON TOP for prose only — the numbers never depend on it.
Research tool, not advice — no buy/sell/hold.
"""
